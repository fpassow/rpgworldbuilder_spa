/*
 * Stateless bag of functions. Each takes the model and draws a different sort of view, 
 *   and wires the controller's public "action" methods into it's events.
 *
 * standardView(model, controller)
 * importView(model, controller)
 */

function Views() {

    this.standardView(model, controller) {




    }



    this.importView(model, controller) {


    }



}